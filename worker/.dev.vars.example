# Required: Your secret API key for authenticating uploads from the mDrop CLI.
# Choose any strong random string (e.g., generate one with: openssl rand -hex 32)
# You'll use this same key when running `mdrop init` to connect the CLI.
# Uploads are blocked until this is set.
API_KEY=
